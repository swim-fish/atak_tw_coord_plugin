<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:ignore="GoogleAppIndexingWarning">

    <!--
        Permissions: the plugin reuses ATAK's own location stream via
        MapView.getSelfMarker(), so it does NOT request ACCESS_FINE_LOCATION
        or ACCESS_COARSE_LOCATION here. INTENTIONALLY no INTERNET permission
        is declared - FR-019 (zero outbound communication) is enforced by
        construction at the manifest level.
    -->

    <application
        android:allowBackup="false"
        android:icon="@drawable/ic_tw_coord"
        android:label="@string/app_name"
        android:description="@string/app_desc"
        android:extractNativeLibs="true"
        android:theme="@style/AppTheme">

        <meta-data android:name="plugin-api" android:value="${atakApiVersion}"/>
        <meta-data android:name="app_desc" android:value="@string/app_desc"/>

        <activity android:name="com.atakmap.app.component"
            tools:ignore="MissingClass"
            android:exported="true">
            <intent-filter android:label="@string/app_name">
                <action android:name="com.atakmap.app.component"/>
            </intent-filter>
        </activity>

    </application>
</manifest>
